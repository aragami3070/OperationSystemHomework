= Упражнение №1 «Изменение системных элементов»

Цель занятия: Изучение приемов и методов работы с системными элементами ОС Windows .

2. Проведение эксперимента по изменению системных элементов (параметров элементов экрана):
  - рабочий стол (цвет);

#image("../image/img1.png")

  - окно (цвет окна, цвет шрифта);

#image("../image/img2.png")

  - заголовок активного окна (размер, цвет, шрифт, размер шрифта, цвет шрифта) и заголовок неактивного окна (размер, цвет, шрифт, размер шрифта, цвет шрифта);

  *Ответ:* на Windows 10 их нельзя изменить

  - кнопки управления окном (размер);

#image("../image/img4.png")

  - граница активного окна (размер, цвет);

#image("../image/img3.png")

  - граница неактивного окна (размер, цвет);

#image("../image/img3.png")

  - название панели (размер, шрифт); (*не меняется*)

#image("../image/img5.png")

  - выделенный пункт меню (размер, цвет, шрифт);(*не меняется*)

#image("../image/img6.png")

  - полоса прокрутки (размер);(*не меняется*)

#image("../image/img7.png")

3. Проведение эксперимента с пиктограммами (изменение расстояния между значками):
  - значок (размер, шрифт);

#image("../image/img8.png")

  - интервал между значками (верт.) (размер);

#image("../image/img10.png")

  - интервал между значками (гор.) (размер);

#image("../image/img9.png")

